# Companion Prompt: create_new_script

Use this exact one-command instruction in future chats:

"Use the `create_new_script` skill to generate the script."

Optional extended version:

"Use the `create_new_script` skill to generate the script, and strictly enforce heartbeat progress logging, power-profile hardening with safe restore, Ctrl+C-safe resume markers, and WandB run-id persistence/resume per seed or repeat."
